# ゴルフ軌跡描画 全工程

## 全体フロー

```
映像取得 → 白黒変換 → トリガー検出 → 追跡開始
→ [実測フェーズ] 軌跡点取得・方向判定・平滑化
→ [予測フェーズ] 等速運動＋自由落下で未来位置を予測
→ 座標変換 → 接続 → 再平滑化 → リボン描画 → 外部送信
```

---

# 前処理

---

### ステップ 1: 映像を白黒に変換する

**処理名**: `binalize_frame` → `binarized_by_hsv`

カメラが捉えた色付き映像を「ボールの軌跡だけが白、それ以外は黒」の2値映像に変換する。映像の明るさ情報（輝度）をもとに、暗い部分（背景）を除外し、明るい部分（軌跡）を白として取り出す。最後に白黒を反転させることで、ボール軌跡＝白、背景＝黒の画像が得られる。

```
出力ピクセル =
    255（白）  … 輝度が閾値より高い（ボール）
      0（黒）  … 輝度が閾値以下（背景）
```

---

### ステップ 2: ボールがトリガーラインを通過したか判定する

**処理名**: `find_vertical_line` → `handling_for_stoping`

画面上に設定した横一線の「トリガーエリア」をボールが横切ったかどうかを判定する。白黒映像の中でトリガーエリアを上端から下端まで縦に貫く白い塊があるかを探す。見つかった瞬間に追跡を開始する。

**判定条件**:

```
白い塊の上端 = 0  かつ  白い塊の下端 ≥ エリアの高さ
```

> 「トリガーエリアを完全に縦に貫通する白い塊がある」＝ボールが通過した、と判定する。

---

# 実測フェーズ（毎フレーム繰り返し）

---

### ステップ 3: ボールの軌跡点を1行ずつ取り出す

**処理名**: `locus_info` → `largest_area_box` → `area_boxes_2`

白黒映像の中から「最も大きな白い塊」を選び、その塊を1ピクセル行ずつ横に切り分けて、各行における白い部分の中心x座標を求める。これによって1フレームぶんの軌跡点の列が得られる。

各行 y における中心x座標：

```
x_center = x_min + (x_右端 - x_左端) / 2
```

> 上昇中はy行ごと、水平移動中はx列ごとに上式を適用する。

---

### ステップ 4: 前フレームの最終点に最も近い塊を選ぶ

**処理名**: `handling_for_running`（連結成分の近傍探索部分）

白黒映像には複数の白い塊が現れることがある。そこで「前回のボール位置に最も近い塊」だけを選ぶことで、別の物体を誤って追いかけないようにする。

各塊との距離：

```
d = √(Δx² + Δy²)

Δx =
    0                                            … 前回点がx範囲内
    min(|x_左端 - x_last|, |x_右端 - x_last|)   … それ以外
```

> d が閾値（`distance_threshold`）以下の塊の中から最小のものを採用する。

---

### ステップ 5: ボールの進行方向を判定する

**処理名**: `handling_for_running`（orient 切り替え部分）

ボールの軌跡の形（縦長か横長か）を見て、今ボールが上昇中・水平移動中・下降中のどの状態かを自動判定する。

**縦横比**:

```
R = 塊の高さ（ピクセル） / 塊の幅（ピクセル）
```

| R の値 | 判定された状態 | 軌跡の読み方 |
|--------|-------------|------------|
| R ≥ 2.0 | 上昇中 | 各行のx中心を上から順に取得 |
| R < 2.0 | 水平移動中（最高点付近） | 各列のy中心を左右方向に取得 |
| R > 3.0 | 下降中 | 各行のx中心を下から順に取得 |

---

### ステップ 6: 軌跡の新しい部分だけを追加する

**処理名**: `extend_locus`

毎フレームで取得した点群には「前フレームでもう追加済みの点」が含まれている。この処理は前回の最終点よりも先に進んでいる点だけを抽出して軌跡に追加する。

| 状態 | 追加する点の条件 |
|------|----------------|
| 上昇中 | y < y_last（さらに上の点） |
| 左移動中 | x < x_last（さらに左の点） |
| 右移動中 | x > x_last（さらに右の点） |
| 下降中 | y > y_last（さらに下の点） |

---

### ステップ 7: 最高点（頂点）を確定する

**処理名**: `handling_for_running`（vertex 確定部分）

毎フレームで「これまで見た中で最も画面上方にある点」を更新し続ける。ある時点で前フレームの最高点が今フレームの検出エリアより上にはみ出たとき、それを頂点として確定する。

**頂点確定条件**:

```
y_最高点（前回） < y_検出エリアの上端（今回）
```

> 画面座標では上方向が y の小さい方向。

---

### ステップ 8: 軌跡を滑らかにする（追跡中）

**処理名**: `smoothing_line` → `correct_num_list_carefully`

取得した軌跡点は1ピクセル単位のノイズを含むことがある。移動平均を繰り返し適用（窓幅 3 → 5 → 7 と段階的に広げる）することで滑らかな線にする。

**移動平均（窓幅 N の場合）**:

```
x_i（平滑） = (1/N) × Σ x[i - ⌊N/2⌋ + k]    （k = 0, 1, ..., N-1）
```

> x・y それぞれ独立に適用する。

---

# 予測フェーズ

---

### ステップ 9: 頂点以降の観測値を記録する

**処理名**: `handling_for_running`（`record_xs` / `record_ys` / `record_times` への追記部分）

頂点が確定してから後、フレームが更新されるたびに最新のボール位置と時刻を蓄積していく。この記録が次のステップ（予測計算）の材料になる。

```
記録列 = { (x₁, y₁, t₁), (x₂, y₂, t₂), ..., (xₙ, yₙ, tₙ) }
```

> ここで t_k は頂点確定の瞬間を t=0 とした経過秒数。

---

### ステップ 10: 予測点を計算する

**処理名**: `estimate_pt`

記録が **0.3秒分以上** 蓄積されたとき、物理モデルを使ってボールの未来位置を予測する。

#### 横方向（x）：等速運動モデル

ボールの横移動は一定速度とみなし、最後に観測した点から外挿する。

```
v_x = (xₙ - x₁) / tₙ          … 1秒あたりの横移動量

x_予測 = xₙ + v_x × (t_現在 - tₙ)
```

#### 縦方向（y）：自由落下モデル

ボールの縦移動は重力による放物線とみなし、観測データから重力加速度を逆算する。

```
g = 2 × (yₙ - y₁) / tₙ²       … 観測データから重力加速度を推定

y_予測 = y₁ + (1/2) × g × t_現在²
```

> - y₁ は頂点確定直後の最初の観測y座標（≒ 頂点のy座標）
> - 画面では下方向が +y なので、重力は +g 方向

---

### ステップ 11: 予測モードへ切り替える

**処理名**: `handling_for_running`（`is_missing = True` になる判定部分）

以下のどちらかが成立したとき、実測の追跡をやめて予測点だけを使って描画を続ける。

| 切り替え条件 | 意味 |
|------------|------|
| 白いピクセルの数 < 20 px | ボールが画面から消えた・見えなくなった |
| 前回の更新から 0.3 秒以上経過 | 追跡が止まった（フレームに変化がない） |

---

### ステップ 12: 予測点を軌跡に補間しながら追加する

**処理名**: `add_estimated_pt` → `correct_line`

現在の軌跡の末尾点 (x_last, y_last) から予測点 (x_予測, y_予測) まで、1ピクセルごとに直線補間した点を軌跡に追加する。

**補間の刻み数**:

```
N = max(|x_予測 - x_last|, |y_予測 - y_last|)
```

**補間点**（k = 1, 2, ..., N）:

```
(x_last + Δx × k / N,  y_last + Δy × k / N)
```

> 地平線の高さ y_limit を超えた場合は追加を打ち切る：
> y_予測 > y_limit → 追加停止

---

# 後処理・描画

---

### ステップ 13: 軌跡を「前面」と「背面」に分割する

**処理名**: `get_result`

全軌跡点を先頭から1点ずつ確認し、**初めて y が増加した（下向きに動いた）点**を境に2つに分ける。

```
前面（front） = { Pᵢ | yᵢ ≤ yᵢ₋₁ }    … 上昇部分
背面（back）  = { Pᵢ | yᵢ > yᵢ₋₁ }    … 下降部分＋予測
```

> 前面は実際に飛んでいくボールの軌跡、背面は頂点を超えた後の軌跡（予測を含む）に対応する。

---

### ステップ 14: イベントカメラ座標を表示映像の座標に変換する

**処理名**: `trans_locus`

イベントカメラの解像度・位置は表示するフルHD映像と異なる。設定された「拡縮率」と「センター位置」を使って座標を変換する。

```
x_表示 = x_検出 × r + x_オフセット
y_表示 = y_検出 × r + y_オフセット
```

| パラメータ | 意味 |
|-----------|------|
| `r` | 拡縮率（`resize_rate`） |
| `x_オフセット` | センター位置設定値から算出したX方向ずれ |
| `y_オフセット` | センター位置設定値から算出したY方向ずれ |

---

### ステップ 15: ショット始点を軌跡の先頭につなぐ

**処理名**: `visualize`（`first_front` 接続部分）

イベントカメラが捉え始めるのはボールがトリガーラインを通過した後。それより手前のショット始点からの軌跡（RGBカメラで取得）を先頭につなぐ。

```
前面軌跡 = [ショット始点列] + [イベントカメラ軌跡]
背面軌跡 = [前面軌跡の最終点] + [下降・予測軌跡]
```

---

### ステップ 16: 表示用の平滑化（前面のみ）

**処理名**: `correct_line` + `correct_num_list`

ショット始点とイベントカメラ軌跡を接続した後の前面軌跡に対して、もう一度平滑化を行う。ステップ 8 と同じ移動平均を使うが、窓幅は設定値（`neighbor_count`）に基づく。

```
N_ave = neighbor_count × 2 + 1

x_i（平滑） = (1/N_ave) × Σ x[k]    （k = i - ⌊N_ave/2⌋ ... i + ⌊N_ave/2⌋）
```

---

### ステップ 17: リボン状に描画する

**処理名**: `draw_locus_front` / `draw_locus_back`

各軌跡点に対して「横に広がった短い線」を重ねることで、リボン状の軌跡を描画する。先頭（ショット始点側）が太く、末尾（ボールの現在位置側）が細くなるよう、幅を徐々に縮小する。

**各点 (xᵢ, yᵢ) への描画線**:

```
線の始点 = (xᵢ - sᵢ, yᵢ)
線の終点 = (xᵢ + sᵢ, yᵢ)
```

**幅の減衰**:

```
s_{i+1} = sᵢ - δ
```

| 軌跡の種類 | 減衰量 δ | 特徴 |
|-----------|---------|------|
| 前面（front） | 0.001 | 速く細くなる |
| 背面（back） | 0.0005 | ゆっくり細くなる |

> 前面の減衰は背面の2倍速いため、前面が速く細くなり背面は長く太さを維持する。

---

### ステップ 18: 外部ソフトへ送信する

**処理名**: `drawer.send()` / `osc_client.send()`

描画したリボン画像を **Spout**（映像配信）で他のソフトへ送る。同時に最新のボール末尾座標を **OSC**（通信プロトコル）で送信する。

```
Spout送信: BGR → RGB → RGBA 変換 → sendImage(rgba_bytes, 1920, 1080)

OSC送信:  [ is_back, x_最終点, y_最終点 ]
```

> 前面と背面は別々の Spout チャンネルとして送信され、受信側で合成される。

---

# 全体フロー（まとめ）

```
【ステップ 1】 映像を白黒に変換
        │
        ▼
【ステップ 2】 トリガーライン通過を検出 → 追跡開始
        │
        ▼
  ┌──────────────────────────────────────────────┐
  │            実測フェーズ（毎フレーム）              │
  │                                              │
  │  【ステップ 3】  1行ずつ中心x座標を取得            │
  │  【ステップ 4】  前フレームに最も近い塊を選択        │
  │  【ステップ 5】  縦横比で進行方向を判定             │
  │  【ステップ 6】  新しい点だけを軌跡に追加           │
  │  【ステップ 7】  頂点を確定                      │
  │  【ステップ 8】  軌跡を移動平均で平滑化             │
  └──────────────────────────────────────────────┘
        │
        ▼ 頂点確定後
  ┌──────────────────────────────────────────────┐
  │            予測フェーズ                         │
  │                                              │
  │  【ステップ 9】  頂点後の位置・時刻を記録           │
  │  【ステップ10】  等速運動＋自由落下で未来位置を計算   │
  │                 x_予測 = xₙ + v_x × Δt       │
  │                 y_予測 = y₁ + (1/2) × g × t²  │
  │  【ステップ11】  ボール消失 or 更新停止 → 切り替え  │
  │  【ステップ12】  末尾→予測点まで補間して軌跡に追加   │
  └──────────────────────────────────────────────┘
        │
        ▼
【ステップ13】 頂点で front / back に分割
【ステップ14】 座標変換（イベント座標 → 表示座標）
【ステップ15】 ショット始点と接続
【ステップ16】 移動平均で再平滑化
【ステップ17】 各点に横線を描画（リボン形状）
【ステップ18】 Spout + OSC で外部ソフトへ送信
```
